#include<bits/stdc++.h>
using namespace std;
int main(){
    // #LAMDA FUNCTION 
    // it is sortway to decleare and used the functio (it only used small code in function is requrired)
    // decleation of lamda function  store lamda function in some variable  
      auto sum=[](int x,int y){
        return x+y;
    };
    // used of lamda function while declearing it 
    int x=4;
    int y=5;
cout<<"sum of two number is "<<sum(x,y)<<endl; 
// lamda function mostly used give the yes / No ans 
// it used with combine with other function to conditional call in inbuild function
}