#include<bits/stdc++.h>
using namespace std;

int main(){
    // to check given charter is lower case 
    char a='a';
    bool ans=islower(a);
    // to  check given charcter is uppwer case 
    char ch='A';
    bool res=isupper(ch);
    // to convert lower to upper 
    char low_upp=tolower(ch);
    // to conver t upper from lowewer
    char upp=toupper(a);
    cout<<upp;
stack<int> s;

}